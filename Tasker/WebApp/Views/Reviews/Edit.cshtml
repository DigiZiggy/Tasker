@model ReviewEditViewModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Review</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Review.Rating" class="control-label"></label>
                <input asp-for="Review.Rating" class="form-control" />
                <span asp-validation-for="Review.Rating" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Review.Comment" class="control-label"></label>
                <input asp-for="Review.Comment" class="form-control" />
                <span asp-validation-for="Review.Comment" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Review.TaskId" class="control-label"></label>
                <select asp-for="Review.TaskId" class="form-control" asp-items="Model.TaskSelectList"></select>
                <span asp-validation-for="Review.TaskId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Review.AppUserId" class="control-label"></label>
                <select asp-for="Review.AppUserId" class="form-control" asp-items="Model.AppUserSelectList"></select>
                <span asp-validation-for="Review.AppUserId" class="text-danger"></span>
            </div>
            <input type="hidden" asp-for="Review.Id" />
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
